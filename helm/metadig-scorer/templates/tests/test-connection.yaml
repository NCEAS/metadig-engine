apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metadig-scorer.fullname" . }}-test-connection"
  labels:
    {{- include "metadig-scorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metadig-scorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
