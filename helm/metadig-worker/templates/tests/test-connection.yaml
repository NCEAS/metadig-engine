apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metadig-worker.fullname" . }}-test-connection"
  labels:
    {{- include "metadig-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metadig-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
