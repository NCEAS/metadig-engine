<?xml version="1.0" encoding="UTF-8"?>
<mdq:check xmlns:mdq="https://nceas.ucsb.edu/mdqe/v1" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://nceas.ucsb.edu/mdqe/v1 ../schemas/schema1.xsd">
  <id>coverage.in.arctic.1</id>
  <name>Arctic coverage</name>
  <description>At least one geographic coverage should intersect with the Arctic, which is defined as the area north of 45° N latitude.</description>
  <type>interpretation</type>
  <level>INFO</level>
  <environment>rscript</environment>
  <code><![CDATA[
  library(metadig)

  if (any(na.omit(as.numeric(north_bound_coordinates)) >= 45)) {
    if (any(na.omit(as.numeric(north_bound_coordinates)) > 90)) {
      failure("A north bounding coordinate is out of range. The valid range is 0 to +90.")
    } else {
      success("At least one geographic coverage is in the Arctic.")
    }
  } else {
    failure("No geographic coverage is in the Arctic.")
  }
  ]]></code>
  <selector>
    <name>north_bound_coordinates</name>
    <xpath>//coverage//geographicCoverage/boundingCoordinates/northBoundingCoordinate</xpath>
  </selector>
</mdq:check>
